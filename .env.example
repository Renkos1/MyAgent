# 复制成 .env 再填。.env 已经在 .gitignore 里，不会被提交。
# SAFETY: 真实的 key 只放 .env，不放这里、不放代码、不粘进对话。
#
# 为什么前缀是 AGENT_ 而不是 ANTHROPIC_（2026-09 实测，阶段 5 决定 D7）：
#   Node 的 --env-file 只「补」进程里没有的变量，已经存在的一律不动。
#   ANTHROPIC_* 是 SDK 和 Claude Code 都认的约定名，某些 shell 里本来就有值，
#   于是这个文件被静默忽略、请求打到别的端点、而且不报错。
#   ANTHROPIC_* 仍然接受，但优先级排在 AGENT_* 后面。
#
# 阶段 4 用的是 SMOKE_*（那时只有冒烟脚本读环境）。现在 src/ 也读了，
# 那个名字名不副实，统一改成 AGENT_。

# 必填。provider 的 key。
AGENT_AUTH_TOKEN=

# 必填。DeepSeek 兼容端点填下面这个；官方是 https://api.anthropic.com
AGENT_BASE_URL=https://api.deepseek.com/anthropic

# 必填，而且是闭集 —— 只认这两个名字之一：
#   deepseek-v4-flash            阶段 4 全部探针 + 5 盘录音带都用它
#   claude-haiku-4-5-20251001    DeepSeek 按档位映射到 flash
# 供应商不校验模型名（实测：打错照常回答，请求被别的模型接走且无信号），
# 所以打错时是 src/infra/anthropic/models.ts 挡下来的，不是它。
AGENT_MODEL=deepseek-v4-flash

# 可选。单次响应的 token 上限，1..200000。不设就是 1024。
AGENT_MAX_TOKENS=1024
